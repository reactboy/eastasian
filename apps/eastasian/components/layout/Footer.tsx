import styled from '@emotion/styled';

const StyledFooter = styled.footer`
  text-align: center;
  padding: 40px 0 8px;
  font-size: 8px;
`;

export const Footer = () => {
  return (
    <StyledFooter>
      <p>Made with ❤️ by eastasian from downforce</p>
    </StyledFooter>
  );
};
